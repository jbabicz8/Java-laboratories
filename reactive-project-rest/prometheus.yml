global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'reactive-project-rest'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080'] # dla kontenera bedzie app a nie localhost